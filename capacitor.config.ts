import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cacao.app',
  appName: 'cacao-app',
  webDir: 'dist/adsancacao-app',
  bundledWebRuntime: false
};

export default config;
